q - Counting occurrences of characters in a string using dictionaries.

Answer :

Create an empty dictionary to store the character counts.Iteration. Iterate through each character in the string. Counting for each character:

If the character is already a key in the dictionary, increment its count by 1.
If the character is not found in the dictionary, add it as a new key with a count of 1.